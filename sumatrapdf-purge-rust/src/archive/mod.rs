/// A more robust version of `filter_settings` that splits the file by lines
/// and accumulates each filesetting into an `&[&str]`.

/// Not used for now, but kept in case I modify this program into something bigger in the future
mod main_iter;
